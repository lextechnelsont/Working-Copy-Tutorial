import SwiftUI

// Test version
// Looks like there is a synching issue when you try and force a refresh
// Looks like there's a sync inssue

// It sodesn't look like changes are saved when you're offline?
// Maybe you have to touch a file in WC to trigger sync?
// Looks like sync in WC blows out older changes in Swift Playground
// THIS IS mac-sp
// THIS IS mac-sp

// Changes to this may be dangerous! As they will be lost if you mess it up and go too far ahead. In fact, viewing a file will overwrite changes made to it.
